import { EstilosValidacionDirective } from './estilos-validacion.directive';

describe('EstilosValidacionDirective', () => {
  it('should create an instance', () => {
    const directive = new EstilosValidacionDirective();
    expect(directive).toBeTruthy();
  });
});
